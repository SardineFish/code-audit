{
  "name": "code-audit-app",
  "version": "1.0.0",
  "description": "Electron app for code-audit.",
  "main": "build/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "npx electron-rebuild",
    "start": "npx electron .",
    "build": "tsc",
    "build:watch": "tsc -w"
  },
  "author": "SardineFish",
  "license": "GPL-3.0",
  "devDependencies": {
    "electron": "^6.0.7",
    "electron-rebuild": "^1.8.6"
  },
  "dependencies": {
    "code-audit": "file:../package"
  }
}
